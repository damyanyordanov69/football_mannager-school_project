# Football Manager - Teams CRUD
Това е проект по Системно програмиране, който реализира Windows Forms приложение за управление на футболни отбори (Teams).

## 📌 Описание на проекта
Целта на проекта е да демонстрира връзка между C# приложение и релационна база данни. 
Основни функции:
- Списък на всички отбори (List)
- Добавяне на нов отбор (Create)
- Редактиране на съществуващ отбор (Update)
- Изтриване на отбор със защита/потвърждение (Delete)

## 📌 Архитектура
Проектът е разделен на логически слоеве:
- **UI (Потребителски интерфейс):** Windows Forms форми (`TeamsForm.cs`)
- **Models:** Модели на данните (`Team.cs`)
- **Repositories:** Слой за CRUD операции с параметризирани SQL заявки (`TeamsRepository.cs`)
- **Data:** Логика за достъп до базата данни (`Db.cs`)

## 📌 Технологии
- **Език:** C#
- **Framework:** .NET Framework (Windows Forms)
- **База данни:** MySQL / MariaDB (с пакет MySql.Data)

## 📌 Инсталация и Настройка на база данни
1. Клонирайте хранилището локално.
2. Отворете папката `database/` и изпълнете първо `schema.sql`, а след това `seed.sql` във вашия MySQL сървър.
3. Отворете `App.config` файла във Visual Studio и променете connection string-а с вашите данни (парола за root).
   Пример:
   `Server=localhost;Database=football_manager;Uid=root;Pwd=ВашатаПарола;Charset=utf8;`
4. Стартирайте проекта.